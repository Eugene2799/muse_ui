name: muse_demo
description: "A Demo project for Muse UI."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # muse_ui: ^0.0.1+1 # For remote development
  muse_ui:
    path: ../../packages/muse # For local development

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
